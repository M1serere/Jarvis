# Installer build

1. Put your logo in the project root as `j_logo.png`.
2. Run `.\build_exe.ps1`.
3. The built app will appear in `dist\Jarvis`.
4. The installer will appear in `installer\Output\JarvisSetup.exe`.

Requirements:

- Python with project dependencies
- `PyInstaller`
- `Inno Setup 6`
